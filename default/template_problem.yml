# SYMBOLIC PROBLEM

0:                              # (optional) problem_key defined in case of multiple problems

    objective:                  # (optional) definition of the objective function
        - Minimize(obj)         # Minimize() or Maximize() with a scalar obj expression

    expressions:                # definition of additional expressions (equalities/inequalities)
        - var_1 - var_2 == 0    
        - var_3 >= 0




