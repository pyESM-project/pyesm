# SYMBOLIC PROBLEM

# definition of the objective function (scalar)
objective:
    - Min(sum(a+b)) # minimize the sum of the costs

# definition of the constraints (vector expressions)
constraints: 
    - u @ X + Y == Q  # total flows demand equals total supply
    - u == u  # dummy


    
    