# structure of sets

flows:
    description: flows 
    filters: 
        dispatch: ['Hourly dispatched', 'Yearly dispatched']

flows_aggregated:
    description: flows aggregated
    filters: 
        dispatch: ['Hourly dispatched', 'Yearly dispatched']
