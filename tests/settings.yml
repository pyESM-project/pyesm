log_level: 'info'

paths: 
    units: integration/units
    models: integration/models
    integrated_models: integration/integrated_models

fixtures:
    units: 
        - constants
        - variables_filtering
        - weibull
        - test_structure
        - sets_mirror
        - inverse

    models: 
        - 0a - tech_filters
        - 0b - year_filters
        - 1 - operation
        - 1a - emissions
        - 2a - planning
        - 2b - planning_discounting
        - 3 - lca
        - 3a - lca_it_pt
        - 4 - infra_year_timeslice
        - 4a - storage

    integrated_models:
        - 1_coupled_model
        - 2_lca_twin_problem 

test_methods:
    update_database_and_problem: {force_overwrite: True}
    initialize_problems: {force_overwrite: True}
    run_model: {force_overwrite: True}
    load_results_to_database: {}
    check_model_results: {}

test_methods_integrated:
    update_database_and_problem: {force_overwrite: True}
    initialize_problems: {force_overwrite: True}
    run_model: {force_overwrite: True, integrated_problems: True}
    load_results_to_database: {}
    check_model_results: {}