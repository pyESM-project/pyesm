.. _tutorial-production-planning-sequential:

Sequential problems solution
============================

This tutorial expands the :ref:`tutorial-production-planning` tutorial to show how 
to build a chain of numerical problems solved in a defined order, to split complex 
modelling need into multiple sub-tasks. In this case, the model is built to solve 
a production planning problem starting from total values of production, profit, 
energy and material uses observed in a defined time frame. The tutorial mirrors 
the workflow described in :ref:`model_generation_from_scratch`, illustrating how 
a single problem can be split into multiple sub-problems solved sequentially. 
In this case, the first sub-problem is solved to determine profit, energy and 
material use per unit of product starting from total values; while the second 
sub-problem is formulated exactly as the standard production planning problem.

.. _problem-statement:

.. admonition:: Problem statement

  Let us consider a production system, capable to produce two types of goods: 
  :math:`product_1` and :math:`product_2`, measured in :math:`units`. 

  Within a defined time frame (say, t=0), the system operates by producing a total 
  quantity of products, and generating total profit, consuming energy and material 
  resources as reported in the table below.

  .. math::
    \begin{array}{c|cc}
    & product_1 & product_2 \\
    \hline
        X_0 [units] & 155.2 & 10.3 \\
        C_0 [€]     & 155.2 & 22.8 \\
        E_0 [kWh]   & 263.8 & 36.2 \\
        M_0 [kg]    & 77.6  & 12.4
      \end{array}

  As for the previous tutorials, the system is characterized by the following 
  assumptions:

  - The production levels of the products are *independent*.

  - Production of each product generates constants *profit* generated, *energy* and 
    *material* use per unit of output (i.e., no economies of scale or other non 
    linearities are considered). 

  The objective of the model is to determine the **maximum-profit production plan**, 
  i.e. the quantity of each product produced leading to the overall system maximum 
  profit, considering different scenarios for energy (:math:`E`) and material 
  (:math:`M`) endowments.

  :math:`E = (250, 300) kWh`, :math:`M = (60, 90) kg` 

  However, in this case, the profit, energy and material use per unit of product 
  *are not known a priori*, and must be determined from the total values observed 
  in the time frame t=0.

In the :download:`zip directory <production_planning_sequential.zip>`, the materials 
related to this tutorial are available, including:

- ``notebook.ipynb`` file: Jupyter Notebook with the complete workflow of the 
  tutorial, including code and comments.
- ``concept.xlsx`` file: Excel workbook with the conceptual model definition, 
  resolving the model with the built-in solver. Useful to understand how the 
  model works and the expected results.
- ``model`` directory: model directory generated by CVXlab, containing: model settings 
  (available in both xlsx and YAML formats), sets file ``sets.xlsx``, SQLite file 
  ``database.db``.
    
The user can follow the step-by-step guide below, generating and filling the required 
files autonomously, or can run the provided Jupyter Notebook and rely on the materials 
provided.


Conceptual model definition
---------------------------

Related user guide step: :ref:`conceptual-model-definition`.

With respect to the base tutorial, the problem could be simply defined as a unique 
optimization problem, by adding one additional expression useful to derive unit 
profit, energy and material use per unit of product from the total values observed 
in the time frame t=0 (see the last expression in the problem definition below). 

Specifically, the variable :math:`x_0` (exogenous) represents the total production 
levels of the products in t=0, while the variable :math:`T_0` (exogenous) represents 
the total values of profit, energy and material use in t=0. The matrix :math:`a` is 
defined as endogenous, calculating coefficients of profit, energy and material use 
per unit of product. However, formulating the problem this way implies that the 
variables :math:`c`, :math:`e`, and :math:`m` (profit, energy and material use per 
unit of product) are also endogenous: this implies the matrix product of endogenous 
variables :math:`c` and :math:`x_0` would apprear in the problem definition, making 
the model as nonconvex. 

.. math::
  \begin{aligned}
  \max \quad & (c \cdot x') \\
  \text{s.t.}\quad & e \cdot x' - E \leq 0 \\
  & m \cdot x' - M \leq 0 \\
  & x \geq 0 \\
  & a \cdot diag(x_0) - T_0 = 0
  \end{aligned}

While this formulation is mathematically correct and solvable in CVXlab (see the 
:ref:`tutorial-production-planning-nonlinear` tutorial), it may not be the best 
option for the following reasons:

- In case of large and analytically complex problems, the explicit solution of 
  nonconvex problems may require significant computational resources and may not 
  fail in finding the global optimum.
- In case several data pre-processing steps are required, the definition of the 
  problem may not be straightforward and understandable for users, and therefore 
  hardly maintainable and reusable.

For this reasons, this tutorial shows how to split the problem into two sub-problems 
solved sequentially, where the output of pre-processing problme/s is used as input 
for downstream problem/s. Specifically, the following strategy is here adopted:

- The first problem, named ``data_calibration``, is solved to determine the unit 
  profit, energy and material use per unit of product starting from the total values 
  observed in t=0. The output of this problem (unit profit, energy and material use) 
  is used as input for the second problem (i.e. it is defined as an hybrid variable).
- The second problem, named ``planning_model``, is formulated exactly as the standard 
  production planning problem, using the unit profit, energy and material use per 
  unit of product determined in the previous step.

This section focuses mainly on the changes with respect to the base tutorial. 

.. rubric:: Defining Sets

Sets defined for the base tutorial have not changed.

.. rubric:: Defining Data Tables and related Variables

The following tables summarizes the Data Tables and associated variables for the 
energy system model.

.. list-table:: Data Tables of the tutorial model (modified/new entries in bold)
  :header-rows: 1
  :widths: 20 15 25 40 

  * - Name (sets domain)
    - Type
    - Domain [cardinality]
    - Description
  * - :math:`total_supply_0(p)` (*new*)
    - Exogenous
    - :math:`p \\ [2]`
    - Total supply by product, time 0 (in *units*)
  * - :math:`total_quantities_0(p,a)`  (*new*)
    - Exogenous
    - :math:`p \times a \\ [2 \times 3 = 6]`
    - Total values of profit, energy use, material use, time 0 (*€*, *kWh*, *kg*)
  * - :math:`production(p,e,m)`
    - Endogenous
    - :math:`p \times e \times m \\ [2 \times 2 \times 2 = 8]`
    - Total products output by energy and material availability (in *units*)
  * - :math:`products_{data}(a,p)` (*modified*)
    - **Hybrid**: exogenous in ``planning_model``; endogenous in ``data_calibration``
    - :math:`a \times p \\ [3 \times 2 = 6]`
    - Collects product attributes (coefficients). Multiple variables will be 
      defined based on this table, each related to a specific attribute.
  * - :math:`energy_{avail}(e)`
    - Exogenous
    - :math:`e \\ [2]`
    - Energy availability scenarios (in *kWh*)
  * - :math:`material_{avail}(m)`
    - Exogenous
    - :math:`m \\ [2]`
    - Material availability scenarios (in *kg*)

Notes:

- The new exogenous tables :math:`total_supply_0(p)` and :math:`total_quantities_0(p,a)` 
  are added and are used to determine the profit, energy and material use per unit 
  of product in the new problem ``data_calibration``. 
- The table :math:`products_{data}(a,p)` is modified and defined as a hybrid table, 
  being exogenous in the ``planning_model`` and endogenous in the ``data_calibration`` 
  problem. This table collects coefficients related to products attributes (unit 
  profit, energy use, and material use), which are not known a priori and must be 
  determined from the total values observed in the time frame t=0. 

The variables associated to the Data Tables are summarized in the table below.

.. list-table:: Variables of the tutorial model (modified/new entries in bold)
  :header-rows: 1
  :widths: 10 10 15 15 20 30 

  * - Source Data Table
    - Variable symbol
    - Shape [rows,cols]
    - Intra-problem sets
    - Inter-problem sets
    - Description
  * - :math:`total_supply_0(p)` (*new*)
    - :math:`x_0`
    - :math:`1, p - [1,2]`
    - :math:`-`
    - :math:`-`
    - Products output in t=0 (*unit*)
  * - :math:`total_quantities_0(p,a)` (*new*)
    - :math:`T_0`
    - :math:`a, p - [3,2]`
    - :math:`-`
    - :math:`-`
    - Total values of profit, energy use, material use, time 0 (*€*, *kWh*, *kg*)
  * - :math:`production(p,e,m)`
    - :math:`x`
    - :math:`1, p - [1,2]`
    - :math:`-`
    - :math:`e \times m - [4]`
    - Products output (*unit*)
  * - :math:`products_{data}(a,p)`  (*new*)
    - :math:`a`
    - :math:`a, p - [3,2]`
    - :math:`-`
    - :math:`-`
    - Specific attributes per product (*€/unit*, *kWh/unit*, *kg/unit*)
  * - :math:`products_{data}(a,p)`
    - :math:`c`
    - :math:`1, p - [1,2]`
    - :math:`-`
    - :math:`-`
    - Specific profit per product (*€/unit*)
  * - :math:`products_{data}(a,p)`
    - :math:`e`
    - :math:`1, p - [1,2]`
    - :math:`-`
    - :math:`-`
    - Specific energy use per product (*kWh/unit*)
  * - :math:`products_{data}(a,p)`
    - :math:`m`
    - :math:`1, p - [1,2]`
    - :math:`-`
    - :math:`-`
    - Specific material use per product (*kg/unit*)
  * - :math:`energy_{avail}(e)`
    - :math:`E`
    - :math:`1, 1`
    - :math:`-`
    - :math:`e - [2]`
    - Energy endowment (*kWh*)
  * - :math:`material_{avail}(m)`
    - :math:`M`
    - :math:`1, 1`
    - :math:`-`
    - :math:`m - [2]`
    - Material endowment (*kg*)

.. rubric:: Defining Problem and related Expressions

With respect to the base tutorial, the two sub-problems are defined here.

.. math::
  \begin{aligned}
  &\textbf{Sub-problem } \texttt{data\_calibration}: \\[4pt]
  &\quad a \cdot diag(x_0) - T_0 = 0 \\
  &\textbf{Sub-problem } \texttt{planning\_model}: \\[4pt]
  &\quad \max_{x} \quad c \cdot x' \\
  &\quad \text{s.t.} \quad e \cdot x' - E \leq 0 \\[4pt]
  &\quad\phantom{\text{s.t.}\;} m \cdot x' - M \leq 0 \\
  &\quad\phantom{\text{s.t.}\;} x \geq 0
  \end{aligned}

Notice that:

- The two problems are solved sequentially, in the following order: ``data_calibration`` 
  -> ``planning_model``. The first problem is solved to determine the unit profit, 
  energy and material use per unit of product, while the second problem is formulated 
  exactly as the standard production planning problem, relying on unit profit, 
  energy and material use per unit of product determined in the previous step.
- Data Table :math:`products_{data}(a,p)` is defined as a hybrid table, being 
  endogenous in the ``data_calibration`` problem and exogenous in the ``planning_model`` 
  problem. This way, the output of the first problem is used as input for the 
  second problem.


Generation of model directory
-----------------------------

| Related user guide step: :ref:`generation-of-model-directory`
| Related API: :py:func:`cvxlab.create_model_dir`

This step is **identical** to the base tutorial. Refer to the corresponding
section of :ref:`tutorial-production-planning` for details.


Fill model setup file(s)
------------------------

Related user guide step: :ref:`fill-model-setup-files`

Only the modified or new parts of the setup files are shown below.

.. rubric:: Modified and new Data Tables and Variables

The full updated ``structure_variables.yml`` is shown below. The two hybrid
tables and the new variables are annotated with inline comments.

.. tabs::

  .. group-tab:: YAML

    File: ``structure_variables.yml``

    .. code-block:: yaml

      # NEW: total supply t=0
      total_supply_0:
        description: total products supply, time 0

        type: exogenous
        coordinates: [Products]
        variables_info:
            x_0:
                Products:
                    dim: cols

      # NEW: total quantities t=0
      total_quantities_0:
          description: total values of profit, energy use, material use, time 0
          type: exogenous
          coordinates: [Products, Attributes]
          variables_info:
              T_0:
                  Products:
                      dim: cols
                  Attributes:
                      dim: rows

      production:
          description: products supply
          type: endogenous
          coordinates: [Products, Scenarios_energy, Scenarios_material]
          variables_info:
              x:
                  Products: 
                      dim: cols

      # MODIFIED: hybrid data table
      products_data:
          description: attributes of products | profits, energy use, material use
          type: {data_calibration: endogenous, planning_model: exogenous}
          coordinates: [Products, Attributes]
          variables_info:
              a:
                  blank_fill: 1
                  Products: 
                      dim: cols
                  Attributes:
                      dim: rows
              c:
                  Products:
                      dim: cols
                  Attributes:
                      dim: rows
                      filters: {type: profit}
              e:
                  Products:
                      dim: cols
                  Attributes:
                      dim: rows
                      filters: {type: energy}
              m:
                  Products:
                      dim: cols
                  Attributes:
                      dim: rows
                      filters: {type: material}

      energy_avail:
          description: availability scenarios for energy
          type: exogenous
          coordinates: [Scenarios_energy]
          variables_info:
              E:
          
      material_avail:
          description: availability scenarios for material
          type: exogenous
          coordinates: [Scenarios_material]
          variables_info:
              M:

  .. group-tab:: XLSX

    File: ``model_settings.xlsx`` | tab ``structure_variables``

    .. list-table::
        :header-rows: 1
        :align: center

        * - table_key
          - description
          - type
          - coordinates
          - variables_info
          - blank_fill
          - Products
          - Attributes
        * - total_supply_0
          - total products supply, time 0
          - exogenous
          - Products
          - 
          - x_0
          - dim: cols
          -
        * - total_quantities_0
          - total values of profit, energy use, material use, time 0
          - exogenous
          - Products, Attributes
          - T_0
          - 
          - dim: cols
          - dim: rows
        * - production
          - products supply
          - endogenous
          - Products, Scenarios_energy, Scenarios_material
          - x
          - 
          - dim: cols
          - 
        * - products_data
          - attributes of products | all
          - data_calibration: endogenous, planning_model: exogenous
          - Products, Attributes
          - a
          - 1
          - dim: cols
          - dim: rows
        * - products_data
          - attributes of products | profits
          - data_calibration: endogenous, planning_model: exogenous
          - Products, Attributes
          - c
          - 1
          - dim: cols
          - dim: rows, filters: {type: profit}
        * - products_data
          - attributes of products | energy use
          - data_calibration: endogenous, planning_model: exogenous
          - Products, Attributes
          - e
          - 1
          - dim: cols
          - dim: rows, filters: {type: energy}
        * - products_data
          - attributes of products | material use
          - data_calibration: endogenous, planning_model: exogenous
          - Products, Attributes
          - m
          - 1
          - dim: cols
          - dim: rows, filters: {type: material}
        * - energy_avail
          - availability scenarios for energy
          - exogenous
          - Scenarios_energy
          - E
          - 
          - 
          -
        * - material_avail
          - availability scenarios for material
          - exogenous
          - Scenarios_material
          - M
          - 
          - 
          -

Notice that:
 
- The ``products_data`` table is defined as a hybrid table, and for all of its 
  variables, the ``blank_fill`` value is set to 1 to ease the data initialization.
  This way, the user does not need to manually fill the related entries in XLSX/CSV.

.. rubric:: Defining model symbolic problem

The problem file is restructured from a single flat problem into two named
sub-problems. The new ``data_calibration`` is composed by one expression only; the
``planning_model`` sub-problem contains the objective function and constraints.

.. tabs::

  .. group-tab:: YAML

    File: ``problem.yml``

    .. code-block:: yaml

      data_calibration:
        expressions:
            - a @ diag(x_0) - T_0 == 0
        description:
            - calculation of model coefficients

      planning_model:
        objective: 
          - Maximize(c @ tran(x))
        expressions:
          - e @ tran(x) - E <= 0
          - m @ tran(x) - M <= 0
          - x >= 0
        description:
            - maximization of total profit
            - energy use less than endowment
            - material use less than endowment
            - positive products supply

  .. group-tab:: XLSX

    File: ``model_settings.xlsx`` | tab ``problem``

    .. list-table::
        :header-rows: 1
        :align: center

        * - problem_key
          - objective
          - expressions
          - description
        * - data_calibration
          - 
          - a @ diag(x_0) - T_0 == 0
          - calculation of model coefficients
        * - planning_model
          - Maximize(c @ tran(x))
          - 
          - maximization of total profit
        * - planning_model
          - 
          - e @ tran(x) - E <= 0
          - energy use less than endowment
        * - planning_model
          - 
          - m @ tran(x) - M <= 0
          - material use less than endowment
        * - planning_model
          - 
          - x >= 0
          - positive products supply 

Notice that:

- Compared to the base tutorial, the problem file gains an outer level of
  named keys (``data_calibration``, ``planning_model``). In the XLSX format, this is
  reflected by the addition of a ``problem_key`` column.
- Sub-problem ``data_calibration`` has no objective, only an equality expression.
  CVXlab resolves equality-only sub-problems as linear systems.


Model class instance generation
-------------------------------

| Related user guide step: :ref:`generate-model-class-instance`
| Related class constructor: :py:class:`cvxlab.Model`

This step is **identical** to the base tutorial. Refer to the corresponding
section of :ref:`tutorial-production-planning` for details.


Fill sets data (model coordinates)
----------------------------------

Related user guide step: :ref:`fill-sets-data`

No changes are required in this step compared to the base tutorial: the ``sets.xlsx`` 
file remains untouched.


Initialization of data structures
---------------------------------

| Related user guide step: :ref:`data-structures-init`
| Related API: :py:meth:`~cvxlab.Model.initialize_model_environment`

This step is **identical** to the base tutorial. Refer to the corresponding
section of :ref:`tutorial-production-planning` for details.

The ``total_supply_0``, ``total_quantities_0`` and ``products_data`` input data 
tabs are now generated within the input data file as they are the exogenous data 
tables, and are ready to be filled by the user. 


Fill exogenous model data
-------------------------

Related user guide step: :ref:`fill-exogenous-data`

The ``energy_avail`` and ``material_avail`` tabs are **unchanged**. The
``products_data`` tab can be left empty, since ``blank_fill`` is set to 1.
The new ``total_supply_0`` and ``total_quantities_0`` tabs are generated and ready 
to be filled by the user with data defined by the :ref:`problem statement <problem-statement>`. 

.. list-table:: ``input_data.xlsx`` file | tab ``total_supply_0``.
  :header-rows: 1

  * - id
    - Products_Name
    - values
  * - 1
    - product_1
    - 155.2
  * - 2
    - product_2
    - 10.3

.. list-table:: ``input_data.xlsx`` file | tab ``total_quantities_0``.
  :header-rows: 1

  * - id
    - Products_Name
    - Attributes_Name
    - values
  * - 1
    - product_1
    - profit
    - 155.2
  * - 2
    - product_1
    - energy
    - 263.8
  * - 3
    - product_1
    - material
    - 77.6
  * - 4
    - product_2
    - profit
    - 22.8
  * - 5
    - product_2
    - energy
    - 36.2
  * - 6
    - product_2
    - material
    - 12.4

.. list-table:: ``input_data.xlsx`` file | tab ``energy_avail``.
  :header-rows: 1

  * - id
    - Scenarios_energy_Name
    - values
  * - 1
    - low
    - 150
  * - 2
    - high
    - 300

.. list-table:: ``input_data.xlsx`` file | tab ``material_avail``.
  :header-rows: 1

  * - id
    - Scenarios_material_Name
    - values
  * - 1
    - low
    - 60
  * - 2
    - high
    - 90


Initialization of numerical problem(s)
--------------------------------------

| Related user guide step: :ref:`numerical-problem-init`
| Related API: :py:meth:`~cvxlab.Model.refresh_database_and_initialize_problem`

This step is **identical** to the base tutorial. Refer to the corresponding
section of :ref:`tutorial-production-planning` for details.

CVXlab generates **five** CVXPY problem instances in total: one for the 
``data_calibration`` sub-problem, and four for the ``planning_model`` sub-problem, 
corresponding to the Cartesian product of the energy and material sets (:math:`2 \times 2 = 4`).


Solution of numerical problem(s)
--------------------------------

| Related user guide step: :ref:`numerical-problem-run`
| Related API: :py:meth:`~cvxlab.Model.run_model`

In this step, the CVXPY numerical problem(s) stored in the ``Model`` instance are 
solved. Since the problems are solved sequentially, the argument ``solution_mode`` 
must be specified as ``'sequential'``, together with the order of problems solution, 
defined by the ``sequential_solution_chain`` argument. If the latter argument 
is not specified, CVXlab will solve the problems in the order defined by the 
problems key in settings.

.. code-block:: python

  model.run_model(
    solution_mode='sequential',
    sequential_solution_chain=['data_calibration', 'planning_model'],
  )


Export endogenous model data
----------------------------

| Related user guide step: :ref:`export-model-results`
| Related API: :py:meth:`~cvxlab.Model.load_results_to_database`

The export step is **identical** to the base tutorial:

.. code-block:: python

   model.load_results_to_database()

After this step, the endogenous data tables in ``database.db`` contain the solved 
values for all scenarios. This makes the model results available for inspection, 
comparison, or external analysis tools.

.. rubric:: Inspecting the exported results

After export, the SQLite database contains two endogenous tables: ``production``
(optimal output levels :math:`x`) and ``products_data`` (coefficients of profit, 
energy and material use per unit of product, enclosed within the variable :math:`a`). 
