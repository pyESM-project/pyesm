# variables of the model

# table_key:
#     name: table_name # description or full name of the table
#     type: table_type # endogenous, exogenous, constant or dictionary
#                           with keys as problem key and values as strings 
#                           defining variable type related to that problem.
#     coordinates: [a, b, c, d] # list of coordinates with set_key symbols
#     variables_info:
#         var_key_1: # variable name
#             intra: # optional, if variable is filtered within intra-problem set
#                 set: years # set_key symbol
#                 filters: {0: warm-up} # filters for the intra-problem set
#             rows: # rows of the variable
#                 set: a # set_key symbol
#                 filters: {0: Supply} # optional, filters for the rows
#             cols: # columns of the variable
#                 set: b # set_key symbol
#                 filters: {0: Physical} # optional, filters for the columns

# -----------------------------------------------------------------------------
# exogenous variables

Z_TOT:
    name: domestic and import flows # description or full name of the table
    type: exogenous 
    coordinates: [products_by_origin,sectors] # list of coordinates with set_key symbols
    variables_info:
        Z_TOT: # variable name
            rows: # rows of the variable
                set: products_by_origin # set_key symbol
            cols: # columns of the variable
                set: sectors # set_key symbol
        Z_TOT_domestic:
             rows: # rows of the variable
                set: products_by_origin # set_key symbol
                filters: {origin: domestic} # optional, filters for the rows                
             cols: # columns of the variable
                set: sectors # set_key symbol       

Y: 
    name: final demand by origin # description or full name of the table
    type: exogenous 
    coordinates: [products_by_origin, consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        Y: # variable name
            rows: # rows of the variable
                set: products_by_origin # set_key symbol
            cols: # columns of the variable
                set: consumption_categories # set_key symbol
        Y_import:
            rows: # rows of the variable
                set: products_by_origin # set_key symbol
                filters: {origin: import} # optional, filters for the rows                
            cols: # columns of the variable
                set: consumption_categories # set_key symbol

I_ff_exo:
    name: special identity  # description or full name of the table
    type: exogenous 
    coordinates: [products, products_by_origin] # list of coordinates with set_key symbols
    variables_info:
        I_ff_exo: # variable name
            rows: # rows of the variable
                set: products # set_key symbol
            cols: # columns of the variable
                set: products_by_origin # set_key symbol

paniere_df:
    name: quote di paniere di domanda finale  # description or full name of the table
    type: exogenous 
    coordinates: [sectors, consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        paniere_df: # variable name
            rows: # rows of the variable
                set: sectors # set_key symbol
            cols: # columns of the variable
                set: consumption_categories # set_key symbol

V_add: 
    name: valore aggiunto  # description or full name of the table
    type: exogenous 
    coordinates: [sectors] # list of coordinates with set_key symbols
    variables_info:
        V_add: # variable name
            cols: # columns of the variable
                set: sectors # set_key symbol

Taxes_int: 
    name: Taxes-sub  # description or full name of the table
    type: exogenous 
    coordinates: [sectors] # list of coordinates with set_key symbols
    variables_info:
        Taxes_int: # variable name
            cols: # columns of the variable
                set: sectors # set_key symbol

Taxes_fin: 
    name: Taxes-sub  # description or full name of the table
    type: exogenous 
    coordinates: [consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        Taxes_fin: # variable name
            cols: # columns of the variable
                set: consumption_categories # set_key symbol

domanda_interna_agg:
    name: domanda finale interna aggregata # description or full name of the table
    type: exogenous 
    coordinates: [consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        domanda_interna_agg: # variable name
            cols: # columns of the variable
                set: consumption_categories # set_key symbol
# -----------------------------------------------------------------------------
# endogenous variables

Ad:
    name: Matrice dei coefficienti intermedi domestici (Ad)  # description or full name of the table
    type: endogenous 
    coordinates: [sectors, products_by_origin] # list of coordinates with set_key symbols
    variables_info:
        Ad: # variable name
            rows: # rows of the variable
                set: products_by_origin # set_key symbol
                filters: {origin: domestic} # optional, filters for the rows  
            cols: # columns of the variable
                set: sectors # set_key symbol

Z_TOT_agg:
    name:  total flows # description or full name of the table
    type: endogenous 
    coordinates: [products,sectors] # list of coordinates with set_key symbols
    variables_info:
        Z_TOT_agg: # variable name
            rows: # rows of the variable
                set: products # set_key symbol
            cols: # columns of the variable
                set: sectors # set_key symbol

Y_agg: 
    name: final demand  # description or full name of the table
    type: endogenous 
    coordinates: [products, consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        Y_agg: # variable name
            rows: # rows of the variable
                set: products # set_key symbol
            cols: # columns of the variable
                set: consumption_categories # set_key symbol


X_iniziale:
    name: output iniziale # description or full name of the table
    type: endogenous 
    coordinates: [sectors] # list of coordinates with set_key symbols
    variables_info:
        X_iniziale: # variable name
            rows:
                set: sectors # set_key symbol

tax_coefficient:
    name: tax coefficient # description or full name of the table
    type: endogenous 
    coordinates: [consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        tax_coefficient: # variable name
            cols: 
                set: consumption_categories # columns of the variable
     
df_dom:
    name: domanda finale interna domestica # description or full name of the table
    type: endogenous 
    coordinates: [products_by_origin] # list of coordinates with set_key symbols
    variables_info:
        df_dom: # variable name
            rows: # columns of the variable
                set: products # set_key symbol
                filters: {origin: domestic} # optional, filters for the rows  
X:
    name: output simulato # description or full name of the table
    type: endogenous 
    coordinates: [products_by_origin] # list of coordinates with set_key symbols
    variables_info:
        X: # variable name
            rows: # columns of the variable
                set: products_by_origin # set_key symbol
                filters: {origin: domestic} # optional, filters for the rows



domanda_finale_int_tot:
    name:  domanda finale interna totale # description or full name of the table
    type: endogenous 
    coordinates: [products,consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        domanda_finale_int_tot: # variable name
            rows: # rows of the variable
                set: products # set_key symbol
            cols: # columns of the variable
                set: consumption_categories # set_key symbol


coeff_importazione_domanda_finale:
    name:  domanda finale interna totale # description or full name of the table
    type: endogenous 
    coordinates: [products_by_origin,consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        coeff_importazione_domanda_finale: # variable name
            rows: # rows of the variable
                set: products_by_origin # set_key symbol
                filters: {origin: import} # optional, filters for the rows  
            cols: # columns of the variable
                set: consumption_categories # set_key symbol

domanda_importazioni_finali:
    name:  domanda di importazioni finali # description or full name of the table
    type: endogenous 
    coordinates: [products_by_origin,consumption_categories] # list of coordinates with set_key symbols
    variables_info:
        domanda_importazioni_finali: # variable name
            rows: # rows of the variable
                set: products_by_origin # set_key symbol
                filters: {origin: import} # optional, filters for the rows  
            cols: # columns of the variable
                set: consumption_categories # set_key symbol
# -----------------------------------------------------------------------------
# constants variables

Identity_matrix:
    name: Identity_matrix # description or full name of the table
    type: constant # endogenous, exogenous, constant or dictionary.
    coordinates: [sectors, products] # list of coordinates with set_key symbols
    variables_info:
      Identity_matrix:
            value: identity
            rows: # rows of the variable
                set: products # set_key symbol
            cols: # columns of the variable
                set: sectors # set_key symbol

sum_vector_sectors:
    name: sum vector # description or full name of the table
    type: constant # endogenous, exogenous, constant or dictionary.
    coordinates: [sectors] # list of coordinates with set_key symbols
    variables_info:
      sum_vector_sectors:
            value: sum_vector
            cols: # columns of the variable
                set: sectors # set_key symbol

sum_vector_products:
    name: sum vector # description or full name of the table
    type: constant # endogenous, exogenous, constant or dictionary.
    coordinates: [products] # list of coordinates with set_key symbols
    variables_info:
      sum_vector_products:
            value: sum_vector
            cols: # columns of the variable
                set: products # set_key symbol




# -----------------------------------------------------------------------------